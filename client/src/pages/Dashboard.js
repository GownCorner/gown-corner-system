import React from 'react';

const Dashboard = () => (
    <div style={{ padding: "2rem" }}>
        <h1>Admin Dashboard</h1>
        <p>Here, admins can monitor transactions and bookings.</p>
        {/* Add more content and functionality here as needed */}
    </div>
);

export default Dashboard;
